server:
  port: 8888
spring:
  application:
    name: erudio-config-server
  cloud:
      config:
         server:
            git:
               uri: git@github.com:joaovictor-jpg/erudio-config-server
               #username: username
               #password: password
               #default-label: main
               search-paths:
               - 'greeting-service*'